import { StyleSheet } from "@react-pdf/renderer";
import { PDF_PAGE_HEIGHT, PDF_PAGE_WIDTH } from "../theme";
import { LD } from "./lockedDesignTokens";

/**
 * Shared visual primitives for locked pages (geometry-free).
 * Pages compose these with their existing fixed heights / row counts.
 */
export const lockedStyles = StyleSheet.create({
  /* ——— Cover ——— */
  cover_page: {
    width: PDF_PAGE_WIDTH,
    height: PDF_PAGE_HEIGHT,
    fontFamily: LD.font.sans,
    backgroundColor: LD.color.paper,
    position: "relative",
  },
  cover_center: {
    position: "absolute",
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    justifyContent: "center",
    alignItems: "center",
    paddingHorizontal: LD.space.xxl,
  },
  cover_title: {
    fontSize: LD.size.display2,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink,
    textAlign: "center",
    letterSpacing: -0.35,
    lineHeight: LD.lineHeight.snug,
    marginBottom: LD.space.lg,
  },
  cover_client: {
    fontSize: LD.size.lead,
    color: LD.color.ink2,
    textAlign: "center",
    lineHeight: LD.lineHeight.normal,
    marginBottom: LD.space.sm,
  },
  cover_date: {
    fontSize: LD.size.bodySm,
    color: LD.color.ink4,
    textAlign: "center",
    lineHeight: LD.lineHeight.relaxed,
  },

  /* ——— Type rhythm ——— */
  eyebrow: {
    fontSize: LD.size.caption,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink3,
    letterSpacing: LD.trackEyebrow,
    textTransform: "uppercase",
    marginBottom: LD.space.xs,
  },
  sectionTitle: {
    fontSize: LD.size.titleSm,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink,
    letterSpacing: -0.15,
    marginBottom: LD.space.sm,
  },
  bandCaption: {
    fontSize: LD.size.caption,
    color: LD.color.ink3,
    lineHeight: LD.lineHeight.relaxed,
    marginBottom: LD.space.sm,
    maxHeight: 22,
  },
  body: {
    fontSize: LD.size.body,
    color: LD.color.ink,
    lineHeight: LD.lineHeight.normal,
    fontFamily: LD.font.sans,
  },
  bodyMuted: {
    fontSize: LD.size.bodySm,
    color: LD.color.ink2,
    lineHeight: LD.lineHeight.relaxed,
    fontFamily: LD.font.sans,
  },
  bodyQuiet: {
    fontSize: LD.size.bodySm,
    color: LD.color.ink3,
    lineHeight: LD.lineHeight.relaxed,
    fontFamily: LD.font.sans,
  },
  caption: {
    fontSize: LD.size.caption,
    color: LD.color.ink4,
    lineHeight: LD.lineHeight.normal,
    fontFamily: LD.font.sans,
  },
  labelStrong: {
    fontSize: LD.size.label,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink2,
  },

  /* ——— Dividers ——— */
  divider: {
    height: LD.border.hairline,
    backgroundColor: LD.color.rule,
    marginVertical: LD.space.sm,
  },
  dividerStrong: {
    height: LD.border.hairline,
    backgroundColor: LD.color.ink4,
    marginVertical: LD.space.sm,
  },

  /* ——— Cards ——— */
  card: {
    backgroundColor: LD.color.paper,
    borderRadius: LD.radius.sm,
    borderWidth: LD.border.hairline,
    borderColor: LD.color.rule,
    overflow: "hidden",
  },
  cardFilled: {
    backgroundColor: LD.color.surface,
    borderRadius: LD.radius.sm,
    borderWidth: LD.border.hairline,
    borderColor: LD.color.rule,
    overflow: "hidden",
  },

  /* ——— TOC ——— */
  toc_row: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    paddingVertical: LD.space.xs + 1,
    borderBottomWidth: LD.border.hairline,
    borderBottomColor: LD.color.rule,
  },
  toc_title: {
    flex: 1,
    paddingRight: LD.space.lg,
    fontSize: LD.size.tocTitle,
    fontFamily: LD.font.sans,
    color: LD.color.ink,
    lineHeight: LD.lineHeight.snug,
  },
  toc_page: {
    width: 40,
    textAlign: "right",
    fontSize: LD.size.tocPage,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink3,
  },

  /* ——— Performance snapshot ——— */
  perf_hero: {
    marginBottom: LD.space.lg,
    justifyContent: "center",
  },
  perf_score: {
    fontSize: LD.size.metricHero,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink,
    letterSpacing: -1.2,
    lineHeight: LD.lineHeight.tight,
  },
  perf_status: {
    fontSize: LD.size.lead,
    color: LD.color.ink2,
    marginTop: LD.space.sm,
    lineHeight: LD.lineHeight.normal,
    fontFamily: LD.font.sans,
  },
  perf_metricCell: {
    flex: 1,
    marginRight: LD.space.sm,
    padding: LD.cardPad.sm,
    backgroundColor: LD.color.surface,
    borderRadius: LD.radius.sm,
    borderWidth: LD.border.hairline,
    borderColor: LD.color.rule,
    justifyContent: "flex-start",
  },
  perf_metricCellLast: {
    flex: 1,
    marginRight: 0,
    padding: LD.cardPad.sm,
    backgroundColor: LD.color.surface,
    borderRadius: LD.radius.sm,
    borderWidth: LD.border.hairline,
    borderColor: LD.color.rule,
    justifyContent: "flex-start",
  },
  perf_metricLabel: {
    fontSize: LD.size.caption,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink3,
    letterSpacing: 0.2,
    textTransform: "uppercase",
    marginBottom: LD.space.xs,
  },
  perf_metricValue: {
    fontSize: LD.size.metricValue,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink,
    letterSpacing: -0.2,
  },
  perf_support: {
    fontSize: LD.size.bodySm,
    color: LD.color.ink3,
    marginTop: LD.space.md,
    lineHeight: LD.lineHeight.relaxed,
    fontFamily: LD.font.sans,
  },

  /* ——— Competitive landscape ——— */
  comp_alertsRow: { flexDirection: "row", marginBottom: LD.space.lg },
  comp_alert: {
    flex: 1,
    marginRight: LD.space.sm,
    padding: LD.cardPad.sm,
    backgroundColor: LD.color.surface,
    borderRadius: LD.radius.sm,
    borderWidth: LD.border.hairline,
    borderColor: LD.color.rule,
    overflow: "hidden",
  },
  comp_alertLast: {
    flex: 1,
    marginRight: 0,
    padding: LD.cardPad.sm,
    backgroundColor: LD.color.surface,
    borderRadius: LD.radius.sm,
    borderWidth: LD.border.hairline,
    borderColor: LD.color.rule,
    overflow: "hidden",
  },
  comp_alertEyebrow: {
    fontSize: LD.size.caption,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink3,
    letterSpacing: LD.trackEyebrow,
    textTransform: "uppercase",
    marginBottom: LD.space.xs,
  },
  comp_alertBody: {
    fontSize: LD.size.bodySm,
    color: LD.color.ink,
    lineHeight: LD.lineHeight.snug,
    fontFamily: LD.font.sans,
  },
  comp_tableTh: {
    flexDirection: "row",
    backgroundColor: LD.color.surface2,
    borderBottomWidth: LD.border.hairline,
    borderBottomColor: LD.color.rule,
    alignItems: "center",
  },
  comp_tableTr: {
    flexDirection: "row",
    borderBottomWidth: LD.border.hairline,
    borderBottomColor: LD.color.rule,
    alignItems: "center",
    overflow: "hidden",
    backgroundColor: LD.color.paper,
  },
  comp_tableTrAlt: {
    flexDirection: "row",
    borderBottomWidth: LD.border.hairline,
    borderBottomColor: LD.color.rule,
    alignItems: "center",
    overflow: "hidden",
    backgroundColor: LD.color.surface,
  },
  comp_thText: {
    fontSize: LD.size.caption,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink3,
    letterSpacing: 0.15,
    textTransform: "uppercase",
  },
  comp_td: { fontSize: LD.size.bodySm, color: LD.color.ink, fontFamily: LD.font.sans },
  comp_tdMuted: { fontSize: LD.size.bodySm, color: LD.color.ink3, fontFamily: LD.font.sans },
  comp_cellName: { width: "34%", paddingRight: LD.space.xs, paddingLeft: LD.space.xs },
  comp_cellNum: { width: "14%" },
  comp_cellRate: { width: "18%" },
  comp_cellRank: { width: "14%" },
  comp_cellFlag: { width: "20%" },

  /* ——— Model breakdown ——— */
  model_highlight: {
    marginBottom: LD.space.lg,
    padding: LD.cardPad.md,
    backgroundColor: LD.color.accentMuted,
    borderRadius: LD.radius.md,
    borderLeftWidth: 3,
    borderLeftColor: LD.color.accent,
    borderTopWidth: 0,
    borderRightWidth: 0,
    borderBottomWidth: 0,
    justifyContent: "center",
    overflow: "hidden",
  },
  model_highlightText: {
    fontSize: LD.size.body,
    color: LD.color.ink,
    lineHeight: LD.lineHeight.normal,
    fontFamily: LD.font.sans,
  },
  model_cell: {
    flex: 1,
    marginRight: LD.space.sm,
    padding: LD.cardPad.sm,
    backgroundColor: LD.color.surface,
    borderRadius: LD.radius.sm,
    borderWidth: LD.border.hairline,
    borderColor: LD.color.rule,
    overflow: "hidden",
  },
  model_cellLast: {
    flex: 1,
    marginRight: 0,
    padding: LD.cardPad.sm,
    backgroundColor: LD.color.surface,
    borderRadius: LD.radius.sm,
    borderWidth: LD.border.hairline,
    borderColor: LD.color.rule,
    overflow: "hidden",
  },
  model_name: {
    fontSize: LD.size.caption,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink2,
    marginBottom: LD.space.xxs + 1,
  },
  model_score: {
    fontSize: LD.size.lead,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink,
    letterSpacing: -0.2,
  },
  model_takeawayLabel: {
    fontSize: LD.size.caption,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink3,
    letterSpacing: LD.trackEyebrow,
    textTransform: "uppercase",
    marginBottom: LD.space.xs,
  },
  model_takeaway: {
    fontSize: LD.size.bodySm,
    color: LD.color.ink3,
    maxHeight: 42,
    overflow: "hidden",
    lineHeight: LD.lineHeight.relaxed,
    fontFamily: LD.font.sans,
  },

  /* ——— AI evidence ——— */
  ev_cardStrength: {
    flex: 1,
    marginRight: LD.space.sm,
    padding: LD.cardPad.md,
    backgroundColor: LD.color.surface,
    borderRadius: LD.radius.md,
    borderWidth: LD.border.hairline,
    borderColor: LD.color.rule,
    borderLeftWidth: 3,
    borderLeftColor: LD.color.accent,
    overflow: "hidden",
  },
  ev_cardRisk: {
    flex: 1,
    marginRight: 0,
    padding: LD.cardPad.md,
    backgroundColor: LD.color.surface,
    borderRadius: LD.radius.md,
    borderWidth: LD.border.hairline,
    borderColor: LD.color.rule,
    borderLeftWidth: 3,
    borderLeftColor: LD.color.risk,
    overflow: "hidden",
  },
  ev_badge: {
    fontSize: LD.size.caption,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink2,
    marginBottom: LD.space.sm,
    letterSpacing: 0.05,
  },
  ev_quote: {
    backgroundColor: LD.color.paper,
    borderRadius: LD.radius.xs,
    paddingVertical: LD.space.sm,
    paddingHorizontal: LD.space.sm + 2,
    marginBottom: LD.space.sm,
    borderLeftWidth: 2,
    borderLeftColor: LD.color.rule,
  },
  ev_quoteText: {
    fontSize: LD.size.body,
    color: LD.color.ink,
    lineHeight: LD.lineHeight.relaxed,
    fontFamily: LD.font.sans,
  },
  ev_micro: {
    fontSize: LD.size.micro,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink4,
    letterSpacing: LD.trackEyebrow,
    textTransform: "uppercase",
    marginBottom: LD.space.xxs + 1,
  },
  ev_microBody: {
    fontSize: LD.size.bodySm,
    color: LD.color.ink2,
    lineHeight: LD.lineHeight.normal,
    fontFamily: LD.font.sans,
    marginBottom: LD.space.xs + 2,
  },
  ev_note: {
    fontSize: LD.size.caption,
    color: LD.color.ink3,
    lineHeight: LD.lineHeight.normal,
    fontFamily: LD.font.sans,
  },

  /* ——— Recommendations ——— */
  rec_col: { flexDirection: "column" },
  rec_card: {
    marginBottom: LD.space.sm,
    padding: LD.cardPad.md,
    backgroundColor: LD.color.surface,
    borderRadius: LD.radius.md,
    borderWidth: LD.border.hairline,
    borderColor: LD.color.rule,
    borderLeftWidth: 3,
    borderLeftColor: LD.color.ink,
    overflow: "hidden",
  },
  rec_cardLast: {
    marginBottom: 0,
    padding: LD.cardPad.md,
    backgroundColor: LD.color.surface,
    borderRadius: LD.radius.md,
    borderWidth: LD.border.hairline,
    borderColor: LD.color.rule,
    borderLeftWidth: 3,
    borderLeftColor: LD.color.ink,
    overflow: "hidden",
  },
  rec_priority: {
    fontSize: LD.size.caption,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink3,
    letterSpacing: LD.trackEyebrow,
    textTransform: "uppercase",
    marginBottom: LD.space.xs,
  },
  rec_title: {
    fontSize: LD.size.title,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink,
    letterSpacing: -0.2,
    lineHeight: LD.lineHeight.snug,
    marginBottom: LD.space.sm,
  },
  rec_insight: {
    fontSize: LD.size.bodySm,
    color: LD.color.ink2,
    lineHeight: LD.lineHeight.normal,
    fontFamily: LD.font.sans,
    marginBottom: LD.space.sm,
  },
  rec_label: {
    fontSize: LD.size.micro,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink4,
    letterSpacing: LD.trackEyebrow,
    textTransform: "uppercase",
    marginBottom: LD.space.xxs,
    marginTop: LD.space.sm,
  },
  rec_labelFirst: {
    fontSize: LD.size.micro,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink4,
    letterSpacing: LD.trackEyebrow,
    textTransform: "uppercase",
    marginBottom: LD.space.xxs,
    marginTop: 0,
  },
  rec_line: {
    fontSize: LD.size.bodySm,
    color: LD.color.ink,
    lineHeight: LD.lineHeight.snug,
    fontFamily: LD.font.sans,
    marginBottom: LD.space.xxs,
  },

  /* ——— Data summary ——— */
  data_bandHeader: {
    flexDirection: "row",
    alignItems: "flex-end",
    justifyContent: "space-between",
    marginBottom: LD.space.xs + 2,
  },
  data_bandTitle: {
    fontSize: LD.size.titleSm,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink,
    letterSpacing: -0.12,
  },
  data_th: {
    flexDirection: "row",
    backgroundColor: LD.color.surface2,
    borderBottomWidth: LD.border.hairline,
    borderBottomColor: LD.color.rule,
    alignItems: "center",
  },
  data_tr: {
    flexDirection: "row",
    borderBottomWidth: LD.border.hairline,
    borderBottomColor: LD.color.rule,
    alignItems: "center",
    overflow: "hidden",
    backgroundColor: LD.color.paper,
  },
  data_trAlt: {
    flexDirection: "row",
    borderBottomWidth: LD.border.hairline,
    borderBottomColor: LD.color.rule,
    alignItems: "center",
    overflow: "hidden",
    backgroundColor: LD.color.surface,
  },
  data_thText: {
    fontSize: LD.size.caption,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink3,
    letterSpacing: 0.12,
    textTransform: "uppercase",
  },
  data_td: {
    fontSize: 7,
    color: LD.color.ink,
    fontFamily: LD.font.sans,
  },
  data_tdMuted: {
    fontSize: 7,
    color: LD.color.ink3,
    fontFamily: LD.font.sans,
  },
  data_sigA: { width: "38%", paddingRight: LD.space.xs, paddingLeft: LD.space.xs },
  data_sigB: { width: "12%" },
  data_sigC: { width: "14%" },
  data_sigD: { width: "12%" },
  data_sigE: { width: "24%" },
  data_compA: { width: "28%", paddingRight: LD.space.xs, paddingLeft: LD.space.xs },
  data_compB: { width: "14%" },
  data_compC: { width: "14%" },
  data_compD: { width: "18%" },
  data_compE: { width: "26%" },

  /* ——— Closing ——— */
  close_block: { marginBottom: LD.space.lg, overflow: "hidden" },
  close_h_exec: {
    fontSize: LD.size.titleSm,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink,
    letterSpacing: -0.1,
    marginBottom: LD.space.sm + 2,
  },
  close_phaseTitle: {
    fontSize: LD.size.bodySm,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink,
    marginBottom: LD.space.xxs + 1,
  },
  close_phaseBody: {
    fontSize: LD.size.bodySm,
    color: LD.color.ink2,
    lineHeight: LD.lineHeight.relaxed,
    fontFamily: LD.font.sans,
  },
  close_h_method: {
    fontSize: LD.size.body,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink3,
    letterSpacing: 0.08,
    textTransform: "uppercase",
    marginBottom: LD.space.xs + 2,
  },
  close_methodBody: {
    fontSize: LD.size.caption,
    color: LD.color.ink3,
    lineHeight: LD.lineHeight.relaxed,
    fontFamily: LD.font.sans,
  },
  close_h_next: {
    fontSize: LD.size.title,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink,
    letterSpacing: -0.2,
    marginBottom: LD.space.sm,
  },
  close_nextBody: {
    fontSize: LD.size.body,
    color: LD.color.ink,
    lineHeight: LD.lineHeight.relaxed,
    fontFamily: LD.font.sans,
  },
  close_logWrap: {
    marginTop: LD.space.md,
    paddingTop: LD.space.sm,
    borderTopWidth: LD.border.hairline,
    borderTopColor: LD.color.rule,
  },
  close_logEyebrow: {
    fontSize: LD.size.micro,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink4,
    letterSpacing: LD.trackEyebrow,
    textTransform: "uppercase",
    marginBottom: LD.space.xs,
  },
  close_logTh: {
    flexDirection: "row",
    backgroundColor: LD.color.surface2,
    borderBottomWidth: LD.border.hairline,
    borderBottomColor: LD.color.rule,
    alignItems: "center",
  },
  close_logTr: {
    flexDirection: "row",
    borderBottomWidth: LD.border.hairline,
    borderBottomColor: LD.color.rule,
    alignItems: "center",
    overflow: "hidden",
    backgroundColor: LD.color.paper,
  },
  close_logCell: { fontSize: LD.size.micro, color: LD.color.ink4, fontFamily: LD.font.sans },
  close_logThText: {
    fontSize: LD.size.micro,
    fontFamily: LD.font.sansBold,
    color: LD.color.ink4,
    letterSpacing: 0.12,
    textTransform: "uppercase",
  },
  close_c1: { width: "10%", paddingLeft: 2 },
  close_c2: { width: "22%", paddingRight: 2 },
  close_c3: { width: "14%" },
  close_c4: { width: "18%" },
  close_c5: { width: "36%" },
});
